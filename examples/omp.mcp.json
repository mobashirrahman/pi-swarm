{
	"mcpServers": {
		"swarm": {
			"command": "npx",
			"args": [
				"-y",
				"pi-swarm-mcp"
			],
			"env": {
				"PI_SWARM_ENV_FILE": "${PI_SWARM_ENV_FILE:-${HOME}/.pi-swarm.env}"
			},
			"timeout": 120000
		}
	}
}
