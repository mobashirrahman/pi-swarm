{
	"name": "pi-swarm",
	"version": "0.1.0",
	"description": "Quota-aware agent-swarm backend: routes concurrent agent turns across multiple AI provider accounts",
	"type": "module",
	"main": "./src/index.ts",
	"engines": {
		"node": ">=20.0.0"
	},
	"scripts": {
		"test": "vitest",
		"test:run": "vitest run",
		"lint": "tsc --noEmit",
		"dev": "tsx src/server.ts",
		"start": "tsx src/server.ts"
	},
	"devDependencies": {
		"@types/node": "^22.20.3",
		"tsx": "^4.23.12",
		"typescript": "^5.9.3",
		"vitest": "^3.2.4"
	}
}
